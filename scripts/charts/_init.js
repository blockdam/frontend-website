let initConfig = function initConfig() {

    let config = {

        margin: {
            top: 0,
            bottom: 0,
            left: 0,
            right: 0
        },
        padding: {
            top: 0,
            bottom: 0,
            left: 0,
            right: 0
        }
    };

    return config;
}